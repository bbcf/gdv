# SQLite databases output directory
sqlite_output_directory : /project/sqlite
# JSON or calculated SQLite output directory
jbrowse_output_directory : /project/jbrowse
# Where calculated SQLite database is
compute_sqlite_scores_database : /project/compute_sqlite_scores/jobs.db
# an url if you want feedback 
feedback_url : http://myServer/myApp/postURL
# where are various databases (chromosome names equivalence, yeat common and alternatives names)
database_link : /project/databases
# the url where jbrowse will take the ressources (urlTemplate for HistogramMeta in trackData.json)
jbrowse_ressource_url : jbrowse/output
