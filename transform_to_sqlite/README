the purpose of this jar file is to transform a BED,WIG,GFF,.... file 
to an sqlite database.
it create a daemon wich run all the time and will execute jobs. 
Jobs are processed from an sqlite database 'jobs.db'.

1- Put the jar file in a directory. 
2- Adjust the paths in the start.sh and stop.sh scripts
3- adjust the conf file in conf directory
4- the first initialisation of the daemon will builts the database :
 create table jobs (file text,trackId text,tmpdir text,extension text,mail text,nrassemblyid text)